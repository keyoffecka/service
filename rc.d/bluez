case "$command_name" in
  start)
    print_msg "Starting Bluetooth daemon"
    
    modprobe ath3k
    modprobe btusb
    
    nohup /usr/bin/bluetoothd >/dev/null 2>&1 &
    success "$?"
    [ "$ret_val" == "0" ] && register $!
  ;;
esac
